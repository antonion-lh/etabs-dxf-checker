# Core dependencies
ezdxf>=1.3.0          # DXF parsing
pandas>=2.0.0         # DataFrames
scipy>=1.13.0         # KDTree spatial queries
numpy>=1.26.0         # Array math

# PDF Report Generation (pure Python, cross-platform)
xhtml2pdf>=0.2.17     # HTML to PDF conversion
reportlab>=4.0.0      # PDF generation engine

# Alternative PDF engine (optional, requires system cairo/pango)
# weasyprint>=62.0

# Windows-only (ETABS COM bridge)
# comtypes>=1.4.0     # Required on Windows to connect to live ETABS v23

# Optional (debug visualisation)
# matplotlib>=3.8.0

# Web Application & Interactive UI
streamlit>=1.30.0
plotly>=5.18.0

# Dev / test
pytest>=8.0.0
